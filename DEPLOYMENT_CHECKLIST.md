# ✅ Чеклист для деплоя

## Перед деплоем (локально)

- [ ] Скопировал `.env.template` в `.env` и заполнил все переменные
- [ ] Заменил `yourdomain.com` в `docker-compose.yml` на реальный домен (4 места)
- [ ] Заменил `your-email@example.com` в `traefik/traefik.yml` на реальный email
- [ ] Закоммитил и запушил все изменения в Git
- [ ] Протестировал локально: `npm run build`

## Настройка сервера Selectel

- [ ] Создал VPS на Selectel (минимум: 2 CPU, 2GB RAM, 20GB SSD)
- [ ] Подключился к серверу по SSH
- [ ] Установил Docker и Docker Compose
- [ ] Настроил firewall (порты 22, 80, 443)
- [ ] Клонировал проект в `/opt/projects/white-wolf-nextjs`
- [ ] Создал `.env` файл на сервере с production данными
- [ ] Создал Docker сеть: `docker network create web`
- [ ] Создал файл `traefik/acme.json` с правами 600

## Настройка домена

- [ ] Настроил A-записи в DNS:
  - `@` или `yourdomain.com` → IP сервера
  - `www` → IP сервера
- [ ] Дождался распространения DNS (проверка: `nslookup yourdomain.com`)
- [ ] Отключил домен от Tilda (если еще активен)

## Запуск

- [ ] Запустил: `docker compose up -d --build`
- [ ] Проверил логи: `docker compose logs -f`
- [ ] Дождался получения SSL сертификатов (1-2 минуты)
- [ ] Открыл сайт в браузере: `https://yourdomain.com`
- [ ] Проверил HTTPS (зеленый замок)
- [ ] Проверил перенаправление с HTTP на HTTPS
- [ ] Протестировал отправку форм

## Опционально (но рекомендуется)

- [ ] Настроил CI/CD через GitHub Actions
  - Добавил секреты: `SERVER_HOST`, `SERVER_USER`, `SSH_PRIVATE_KEY`, `DOMAIN`
- [ ] Настроил автоматические бэкапы (cron)
- [ ] Установил fail2ban для защиты SSH
- [ ] Включил автоматические обновления безопасности
- [ ] Настроил мониторинг (uptime robot, cronitor и т.д.)

## После запуска

- [ ] Проверил логи на ошибки: `docker compose logs`
- [ ] Протестировал все формы на сайте
- [ ] Проверил получение писем на CLINIC_EMAIL
- [ ] Настроил резервное копирование
- [ ] Добавил сайт в поисковые системы (Google Search Console, Yandex Webmaster)

## Полезные команды

```bash
# Обновление проекта
cd /opt/projects/white-wolf-nextjs
git pull origin main
docker compose up -d --build

# Просмотр логов
docker compose logs -f whitewolf

# Перезапуск
docker compose restart

# Статус контейнеров
docker compose ps

# Использование ресурсов
docker stats
```

---

**Если все пункты выполнены - поздравляю, деплой завершен! 🎉**
