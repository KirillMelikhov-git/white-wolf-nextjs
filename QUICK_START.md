# 🚀 Быстрый старт - Форма записи на приём

## ✅ Что готово

Форма записи на приём полностью реализована и интегрирована в проект!

## 📦 Установленные пакеты

Все необходимые пакеты уже установлены:

- ✅ react-hook-form
- ✅ zod
- ✅ @hookform/resolvers
- ✅ react-imask
- ✅ nodemailer
- ✅ @types/nodemailer

## ⚙️ Настройка (ОБЯЗАТЕЛЬНО!)

### 1. Создайте файл `.env.local` в корне проекта

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
CLINIC_EMAIL=clinic-email@example.com
```

### 2. Для Gmail (рекомендуется)

1. Перейдите на https://myaccount.google.com/security
2. Включите двухэтапную аутентификацию
3. Перейдите в "Пароли приложений"
4. Создайте новый пароль приложения
5. Используйте его в `SMTP_PASSWORD`

### 3. Альтернативные сервисы

**Yandex:**

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
```

**Mail.ru:**

```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
```

## 🏃 Запуск

```bash
# Запустите dev-сервер
npm run dev
```

Откройте http://localhost:3000 и прокрутите вниз до секции "Запись на приём"

## 🧪 Тестирование

1. Заполните все поля формы
2. Нажмите "Записаться на приём"
3. Проверьте email клиники (CLINIC_EMAIL)
4. Вы должны получить красиво оформленное письмо с данными заявки

## 📁 Что было создано

### Переиспользуемые компоненты (shared/ui)

- `Button` - кнопка с состояниями loading
- `Input` - поле ввода с валидацией
- `Textarea` - многострочное поле с валидацией

### Entity (entities/appointment-form)

- Модель с Zod схемой валидации
- UI компонент формы с React Hook Form
- TypeScript типы

### Widget (widgets/AppointmentSection)

- Секция с заголовком и формой
- Готова к использованию в любом месте

### API Route (app/api/send-appointment)

- POST endpoint для отправки email
- Валидация на сервере
- Отправка через Nodemailer

## 🎨 Функции формы

- ✅ Валидация всех полей
- ✅ Маска телефона +7 (**_) _**-**-**
- ✅ Показ ошибок под полями
- ✅ Loader при отправке
- ✅ Success/Error уведомления
- ✅ Очистка после успешной отправки
- ✅ Адаптивный дизайн
- ✅ Красивое HTML письмо

## 📝 Поля формы

1. **ФИО** - обязательное, минимум 2 слова
2. **Телефон** - с маской, обязательное
3. **Email** - с валидацией, обязательное
4. **Кличка животного** - обязательное
5. **Описание проблемы** - обязательное, минимум 10 символов

## 🔧 Дополнительная информация

- Подробная настройка SMTP: `SMTP_SETUP.md`
- Полное руководство: `APPOINTMENT_FORM_GUIDE.md`
- README entity: `src/entities/appointment-form/README.md`

## ⚠️ Важно

**Без настройки `.env.local` форма не будет работать!**

Обязательно создайте файл с настройками SMTP перед тестированием.

## 🎉 Готово!

После настройки `.env.local` форма полностью готова к использованию!
