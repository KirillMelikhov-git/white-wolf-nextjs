# 🐾 Руководство по форме записи на приём

## 📋 Что было реализовано

Создана полнофункциональная форма записи на приём в ветеринарную клинику с использованием современных технологий и FSD архитектуры.

## 🏗️ Структура проекта

```
src/
├── shared/ui/
│   ├── Button/              # Переиспользуемая кнопка
│   ├── Input/               # Переиспользуемый input
│   └── Textarea/            # Переиспользуемый textarea
│
├── entities/
│   └── appointment-form/    # Сущность формы записи
│       ├── model/
│       │   ├── schema.ts    # Zod валидация
│       │   ├── types.ts     # TypeScript типы
│       │   └── index.ts
│       └── ui/
│           ├── AppointmentForm.tsx
│           ├── AppointmentForm.module.scss
│           └── index.ts
│
├── widgets/
│   └── AppointmentSection/  # Виджет секции с формой
│       ├── ui/
│       │   ├── AppointmentSection.tsx
│       │   ├── AppointmentSection.module.scss
│       │   └── index.ts
│       └── index.ts
│
└── app/
    ├── api/
    │   └── send-appointment/
    │       └── route.ts     # API endpoint для отправки email
    └── page.tsx             # Интеграция формы
```

## 🎨 Поля формы

1. **ФИО** - обязательное, минимум 2 слова
2. **Контактный телефон** - с маской +7 (**_) _**-**-**
3. **Email** - с валидацией формата
4. **Кличка животного** - обязательное, 2-50 символов
5. **Описание проблемы** - обязательное, минимум 10 символов

## 🚀 Технологии

- **React Hook Form** - управление состоянием формы
- **Zod** - type-safe валидация
- **React IMask** - маска для телефона
- **Nodemailer** - отправка email
- **Next.js API Routes** - серверный endpoint
- **SCSS Modules** - стилизация

## ⚙️ Настройка

### 1. Создайте файл `.env.local`

В корне проекта создайте файл `.env.local` со следующими переменными:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
CLINIC_EMAIL=clinic@example.com
```

### 2. Настройка Gmail (рекомендуется)

1. Перейдите в [Google Account Security](https://myaccount.google.com/security)
2. Включите двухэтапную аутентификацию
3. Перейдите в "Пароли приложений"
4. Создайте новый пароль приложения
5. Используйте его в `SMTP_PASSWORD`

### 3. Альтернативные почтовые сервисы

**Yandex:**

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
```

**Mail.ru:**

```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
```

## 🧪 Тестирование

1. Запустите dev-сервер:

```bash
npm run dev
```

2. Откройте http://localhost:3000

3. Прокрутите страницу до формы "Запись на приём"

4. Заполните и отправьте тестовую заявку

5. Проверьте email клиники

## ✨ Функциональность

### Валидация

- ✅ Клиентская валидация (мгновенная обратная связь)
- ✅ Серверная валидация (безопасность)
- ✅ Показ ошибок под полями

### UX

- ✅ Disabled кнопка во время отправки
- ✅ Loader при отправке
- ✅ Success/Error уведомления
- ✅ Очистка формы после успешной отправки
- ✅ Адаптивный дизайн (mobile-first)

### Email

- ✅ Красивое HTML письмо
- ✅ Fallback на текстовую версию
- ✅ Все данные из формы
- ✅ Дата и время отправки

## 🎯 Использование

Форма автоматически интегрирована в главную страницу после секции с сотрудниками.

Для использования в других местах:

```tsx
import { AppointmentSection } from '@/widgets/AppointmentSection';

export default function MyPage() {
  return (
    <div>
      <AppointmentSection />
    </div>
  );
}
```

Или только форму без обертки:

```tsx
import { AppointmentForm } from '@/entities/appointment-form';

export default function MyPage() {
  return (
    <div>
      <AppointmentForm />
    </div>
  );
}
```

## 🔒 Безопасность

- ✅ Валидация на клиенте и сервере
- ✅ Защита от XSS
- ✅ `.env.local` в `.gitignore`
- ✅ Использование паролей приложений
- ✅ HTTPS для production

## 📱 Адаптивность

Форма полностью адаптивна:

- Desktop: 2 колонки для полей
- Mobile: 1 колонка для полей
- Все элементы масштабируются

## 🐛 Troubleshooting

### Ошибка отправки email

1. Проверьте переменные окружения в `.env.local`
2. Убедитесь, что используете пароль приложения (для Gmail)
3. Проверьте настройки SMTP в почтовом сервисе
4. Перезапустите dev-сервер после изменения `.env.local`

### Ошибки валидации

1. Проверьте формат телефона: +7 (**_) _**-**-**
2. ФИО должно содержать минимум 2 слова
3. Email должен быть в корректном формате
4. Описание минимум 10 символов

## 📝 Дополнительная информация

Подробная настройка SMTP: см. файл `SMTP_SETUP.md`

## 🎉 Готово!

Форма полностью готова к использованию. Настройте SMTP и начните принимать заявки!
