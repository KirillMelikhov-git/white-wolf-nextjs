# 🗺️ Быстрое исправление проблемы с Яндекс картами на сервере

## Проблема

В задеплоенной версии не работает карта в футере, потому что не установлен API ключ для Яндекс Карт.

## ✅ Решение (5 минут)

### Шаг 1: Получить API ключ Яндекс Карт

1. Перейди на https://developer.tech.yandex.ru/
2. Войди в аккаунт Яндекс
3. Перейди в "Мои сервисы" → "JavaScript API"
4. Нажми "Подключить API" → "JavaScript API и HTTP Геокодер"
5. Нажми "Получить ключ"
6. Укажи название: "White Wolf Clinic"
7. **ВАЖНО:** В поле "Источники запросов" укажи свой домен (без `https://`), например: `yourdomain.com`
8. Скопируй полученный ключ

### Шаг 2: Добавить ключ в `.env` на сервере

```bash
# Подключись к серверу
ssh root@your-server-ip

# Перейди в директорию проекта
cd /opt/projects/white-wolf-nextjs

# Открой .env файл
nano .env

# Добавь строку (замени на свой ключ):
NEXT_PUBLIC_YANDEX_MAPS_API_KEY=твой-ключ-здесь

# Сохрани: Ctrl+O, Enter, Ctrl+X
```

### Шаг 3: Перезапустить контейнер

```bash
# Пересобрать и перезапустить
docker compose up -d --build

# Проверить логи
docker compose logs -f whitewolf

# Проверить, что переменная установлена
docker compose exec whitewolf env | grep YANDEX
```

### Шаг 4: Проверить работу

1. Открой сайт: `https://yourdomain.com`
2. Прокрути вниз до футера
3. Нажми "📍 посмотреть на карте"
4. Карта должна загрузиться!

## 🔍 Проверка в браузере

Открой консоль браузера (F12 → Console):

- ✅ **Должно быть:** "Карта создана успешно"
- ❌ **Если ошибка:** "API ключ не найден!" - вернись к Шагу 2

## 📝 Изменения в проекте

Были обновлены следующие файлы (уже в репозитории):

- ✅ `docker-compose.yml` - добавлена переменная `NEXT_PUBLIC_YANDEX_MAPS_API_KEY`
- ✅ `ENV_TEMPLATE.md` - подробная инструкция по получению API ключа
- ✅ `DEPLOYMENT.md` - обновлена инструкция по деплою
- ✅ `QUICK_START.md` - добавлена переменная для локальной разработки

## ⚠️ Важно

- Переменная **ОБЯЗАТЕЛЬНО** должна начинаться с `NEXT_PUBLIC_` (это требование Next.js для клиентских переменных)
- Домен должен быть добавлен в разрешенные источники в консоли Яндекс
- Бесплатный лимит: 25,000 запросов/день (этого более чем достаточно)

## 🆘 Если что-то пошло не так

### Карта всё равно не работает

```bash
# Проверь переменную
docker compose exec whitewolf env | grep YANDEX

# Если пусто - повтори Шаг 2
# Если есть - проверь консоль браузера (F12)
```

### Ошибка "Не удалось загрузить карту"

Возможные причины:

1. API ключ неправильно скопирован (есть пробелы или неполный)
2. Домен не добавлен в разрешенные источники
3. Превышен лимит запросов (маловероятно)

**Решение:**

1. Пересоздай API ключ в консоли Яндекс
2. Убедись, что домен указан правильно (без `https://` и `/`)
3. Скопируй ключ заново (весь, без пробелов)

### В консоли браузера ошибка CORS

Это значит, что домен не добавлен в разрешенные источники:

1. Перейди в консоль разработчика Яндекс
2. Найди свой ключ
3. Редактируй источники запросов
4. Добавь свой домен (например, `yourdomain.com`)
5. Сохрани

## 📚 Дополнительная информация

- **Полная инструкция:** `ENV_TEMPLATE.md`
- **Документация API:** https://yandex.ru/dev/maps/jsapi/doc/2.1/
- **Консоль разработчика:** https://developer.tech.yandex.ru/

---

**После исправления карты будут работать! 🎉**
